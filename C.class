package premium;

import java.io.ByteArrayOutputStream;

public class C {
  public static boolean a;
  
  public static StringBuffer b(Object paramObject, int paramInt) {
    return (g() < 0) ? ((StringBuffer)paramObject).append(paramInt) : null;
  }
  
  public static int c(Object paramObject) {
    return (D.c() < 0) ? ((String)paramObject).length() : 0;
  }
  
  public static String d(short[] paramArrayOfshort, int paramInt1, int paramInt2, int paramInt3) {
    char[] arrayOfChar = new char[paramInt2];
    for (byte b = 0; b < paramInt2; b++)
      arrayOfChar[b] = (char)(char)(paramArrayOfshort[paramInt1 + b] ^ paramInt3); 
    return new String(arrayOfChar);
  }
  
  public static String e(String paramString) {
    byte b = 0;
    String str1 = D.f();
    String str2 = D.f();
    int i;
    for (i = 0;; i++) {
      if (i >= 15) {
        ByteArrayOutputStream byteArrayOutputStream = new ByteArrayOutputStream(c(paramString) / 2);
        for (i = 0;; i += 2) {
          byte[] arrayOfByte;
          if (i >= c(paramString)) {
            arrayOfByte = B.g(byteArrayOutputStream);
            int j = arrayOfByte.length;
            int k = c(str2);
            for (i = 0;; i++) {
              if (i >= j) {
                for (i = b;; i = c(D.f()) + 1) {
                  if (i >= arrayOfByte.length)
                    return new String(arrayOfByte); 
                } 
                break;
              } 
              arrayOfByte[i] = (byte)(byte)(arrayOfByte[i] ^ D.d(str2, i % k));
            } 
            break;
          } 
          A.h(byteArrayOutputStream, D.b(str1, D.d(arrayOfByte, i)) << 4 | D.b(str1, D.d(arrayOfByte, i + 1)));
        } 
        break;
      } 
      str1 = A.f(A.c(A.c(new StringBuffer(), str1), B.d(i)));
      str2 = A.f(b(A.c(new StringBuffer(), str2), (int)(B.i() * 10) ^ i));
    } 
  }
  
  public static void f(Object paramObject) {
    if (F.b() < 0)
      paramObject.notifyAll(); 
  }
  
  public static int g() {
    return 0xFFE54D68 ^ F.e("");
  }
}