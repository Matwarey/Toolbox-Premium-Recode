package premium;

import android.os.Handler;
import java.io.ByteArrayOutputStream;

public class A {
  public static boolean a;
  
  public static boolean b(Object paramObject1, Object paramObject2, long paramLong) {
    return (F.b() <= 0) ? ((Handler)paramObject1).postDelayed((Runnable)paramObject2, paramLong) : false;
  }
  
  public static StringBuffer c(Object paramObject1, Object paramObject2) {
    return (B.c() <= 0) ? ((StringBuffer)paramObject1).append((String)paramObject2) : null;
  }
  
  public static String d(String paramString) {
    byte b = 0;
    String str1 = D.f();
    String str2 = D.f();
    int i;
    for (i = 0;; i++) {
      if (i >= 15) {
        ByteArrayOutputStream byteArrayOutputStream = new ByteArrayOutputStream(C.c(paramString) / 2);
        for (i = 0;; i += 2) {
          byte[] arrayOfByte;
          if (i >= C.c(paramString)) {
            arrayOfByte = B.g(byteArrayOutputStream);
            int j = arrayOfByte.length;
            int k = C.c(str2);
            for (i = 0;; i++) {
              if (i >= j) {
                for (i = b;; i = C.c(D.f()) + 1) {
                  if (i >= arrayOfByte.length)
                    return new String(arrayOfByte); 
                } 
                break;
              } 
              arrayOfByte[i] = (byte)(byte)(arrayOfByte[i] ^ D.d(str2, i % k));
            } 
            break;
          } 
          h(byteArrayOutputStream, D.b(str1, D.d(arrayOfByte, i)) << 4 | D.b(str1, D.d(arrayOfByte, i + 1)));
        } 
        break;
      } 
      str1 = f(c(c(new StringBuffer(), str1), B.d(i)));
      str2 = f(C.b(c(new StringBuffer(), str2), (int)(B.i() * 10) ^ i));
    } 
  }
  
  public static int e() {
    return 0x98967F ^ F.e("");
  }
  
  public static String f(Object paramObject) {
    return (F.b() < 0) ? ((StringBuffer)paramObject).toString() : null;
  }
  
  public static String g(short[] paramArrayOfshort, int paramInt1, int paramInt2, int paramInt3) {
    char[] arrayOfChar = new char[paramInt2];
    for (byte b = 0; b < paramInt2; b++)
      arrayOfChar[b] = (char)(char)(paramArrayOfshort[paramInt1 + b] ^ paramInt3); 
    return new String(arrayOfChar);
  }
  
  public static void h(Object paramObject, int paramInt) {
    if (F.b() < 0)
      ((ByteArrayOutputStream)paramObject).write(paramInt); 
  }
}
